# RAG 部署核心依赖
fastapi>=0.100.0
uvicorn>=0.23.0
sentence-transformers>=2.2.2
faiss-cpu>=1.7.4
pydantic>=2.0.0
transformers>=4.38.0
accelerate>=0.27.0

# 注意：PyTorch 引擎由于 50 系显卡的特殊性，
# 请严格按照 README.md 中的 Nightly 版本手动安装，不要写在这里以免引起冲突。
